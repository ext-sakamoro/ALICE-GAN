[package]
name = "alice-gan"
version = "1.0.0"
edition = "2021"
authors = ["Moroya Sakamoto"]
license = "MIT"
description = "Generative Adversarial Networks — pure Rust GAN framework"

[lib]
crate-type = ["rlib"]
